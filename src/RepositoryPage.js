import React from 'react';
import RepoPageHeader from './components/RepoPageHeader'

const RepositoryPage = (props) => {
  return (
      <RepoPageHeader {...props} />
  )
}

export default RepositoryPage;
